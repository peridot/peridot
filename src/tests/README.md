# Tests?
No these aren't the real tests for the language, this are crappy scripts i wrote just to test the binary everytime i make modifications on a specific thing.

For the real tests see the `tests/` directory in the root of the repository.

TODO: Remove some of the very old useless scripts.
